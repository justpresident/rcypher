Repository guidance for Claude Code lives in AGENTS.md — the tool-agnostic
source of truth, shared with every other agent and contributor. Import it here
so Claude Code loads it automatically at session start:

@AGENTS.md
